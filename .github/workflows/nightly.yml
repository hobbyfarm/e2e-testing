# purpose: execute every night the test suite on a given environment

name: Nightly

on:
  schedule:
    - cron:  '0 3 * * *'
  workflow_dispatch: {}

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Install Node.js (LTS)
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Setup NPM cache cache
        id: cache-nodemodules
        uses: actions/cache@v3
        env:
          cache-name: cache-node-modules
        with:
          path: node_modules
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
      - name: Install NPM packages
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npx playwright test --trace on
        env:
          HOBBYFARM_ADMIN_UI_URL: https://someurl
          HOBBYFARM_ADMIN_UI_USR: admin
          HOBBYFARM_ADMIN_UI_PWD: admin
          HOBBYFARM_UI_URL: https://someurl
          HOBBYFARM_GARGANTUA_URL: https://someurl
          HOBBYFARM_ADMIN_UI_PWD_NEW: admin
      - uses: actions/upload-artifact@v3
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
